## 1. Retry 分类与退避策略

- [x] 1.1 在 `pipeline/model_client.py` 中实现“可重试错误”判定函数（网络类、超时、HTTP 429、HTTP 5xx）。
- [x] 1.2 在 `pipeline/model_client.py` 中实现“不可重试错误”快速失败路径（HTTP 4xx 且非 429、响应格式错误）。
- [x] 1.3 将 `chat_with_retry` 的重试预算固定为总尝试 3 次（含首次），并确保语义清晰。
- [x] 1.4 按 `base_delay=1s` 实现指数退避 `1 -> 2 -> 4`，并加入 `max_delay=20s` 封顶。
- [x] 1.5 实现 `1.0~1.5x` 正向 jitter（只加不减），并在单元逻辑中保证不会小于基准 delay。

## 2. 分析阶段失败隔离与日志

- [x] 2.1 在 `pipeline/pipeline.py` 的 analyze 循环中增加 item 级异常处理，保证单条失败后继续后续 item。
- [x] 2.2 为重试日志补齐上下文字段：item `title/url`、attempt 进度、错误类型、计划等待时长。
- [x] 2.3 为终止失败日志补齐“放弃该条并继续处理”的明确语义。
- [x] 2.4 确认不修改 `step_collect / step_organize / step_save` 的行为边界。

## 3. 验证与回归检查

- [x] 3.1 构造可重试失败样例（网络异常、超时、429、5xx），验证会触发重试且不超过 3 次总尝试。
- [x] 3.2 构造不可重试失败样例（400/401/403），验证首失败即终止该条且不重试。
- [x] 3.3 构造响应格式错误样例，验证不重试并按 item 级失败隔离继续批处理。
- [x] 3.4 运行小批量 pipeline（例如 `--limit 10`）检查日志字段完整性与整体流程连续性。
